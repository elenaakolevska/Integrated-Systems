@model List<Courses.Domain.DomainModels.Student>

@{
    ViewData["Title"] = "Index";
}

<div class="container">

    <div class="row">
        <a class="btn btn-success" style="width:200px; margin-left:28px" asp-action="Create">Insert New Student</a>
    </div>

    @for (int i = 0; i < Model.Count; i++)
    {
        var item = Model[i];

        @if(i%3 == 0)
        {
            @:<div class="row">
        }

                <div class="col-3 m-3">

                    <div class="card" style="width: 20rem; height: 30rem;">

                        <div class="card-body">
                            <h3 class="card-title">@item.Name</h3>
                            <p class="card-text">@item.Surname</p>
                            <p class="card-text">@item.DateOfBirth</p>
                            <p class="card-text">@item.Index</p>
                            <p class="card-text">@item.EnrollmentYear</p>

                        </div>

                        <div class="card-footer">
                            <a asp-controller="Students" asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">Details</a>
                            <a asp-controller="Students" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-secondary">Edit</a>
                            <a asp-controller="Students" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                        </div>
                    </div>

                </div>

        @if(i%3 == 2) 
        {
            @:</div>    
        }
    }

</div>
